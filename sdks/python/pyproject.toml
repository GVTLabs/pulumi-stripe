[project]
  name = "pulumi_stripe"
  description = "A Pulumi provider dynamically bridged from stripe."
  dependencies = ["parver>=0.2.1", "pulumi>=3.247.0,<4.0.0", "semver>=2.8.1", "typing-extensions>=4.11,<5; python_version < \"3.11\""]
  readme = "README.md"
  requires-python = ">=3.9"
  version = "0.2.3"
  [project.urls]
    Repository = "https://github.com/stripe/terraform-provider-stripe"

[build-system]
  requires = ["setuptools>=61.0"]
  build-backend = "setuptools.build_meta"

[tool]
  [tool.setuptools]
    [tool.setuptools.package-data]
      pulumi_stripe = ["py.typed", "pulumi-plugin.json"]
